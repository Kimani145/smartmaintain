# Contributing

1. Fork the repository.
2. Create a feature branch: `git checkout -b feature/name`.
3. Ensure no `any` types or `ts-ignore` flags are used.
4. Run `npm run lint` and `npm run build` locally.
5. Submit a PR for review.
